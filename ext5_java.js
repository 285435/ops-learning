// === JAVA EXT5 ===
java_ext5_topics: [
  {
    "id": "java-ext5-new-features",
    "title": "Java 17/21 LTS/22 新特性深入",
    "level": "高级",
    "content": "**Java 17 LTS 核心特性**\n- **Record 类（Java 16 正式）**：不可变数据载体类，自动生成构造器、getter、equals、hashCode、toString。通过 public record Point(int x, int y) {} 一行声明，所有字段为 final，适合 DTO、值对象。可自定义构造器添加校验，支持嵌套 Record、实现接口，但不能继承其他类，也不能被继承（隐式 final）。\n- **Sealed Class（密封类，Java 17 正式）**：通过 permits 精确控制哪些类可以继承，实现封闭继承体系。public sealed class Shape permits Circle, Rectangle, Triangle {} 只有指定子类可继承。子类必须声明为 final、sealed 或 non-sealed（打破密封）。配合模式匹配可实现穷尽的类型检查。\n- **Pattern Matching for instanceof（Java 16 正式）**：省去强制类型转换，if (obj instanceof String s) { s.length(); } 直接在条件中绑定变量 s。\n- **Switch 模式匹配（Java 17 预览，21 正式）**：支持类型模式、null 处理、守卫模式。可返回值（表达式 switch），不再需要 break，支持箭头语法。\n- **Text Block（文本块，Java 15 正式）**：三引号 \"\"\" 多行字符串，自动处理缩进和换行，适合 JSON、SQL、HTML 嵌入。\n\n**Java 21 LTS 革命性特性**\n- **虚拟线程（Virtual Threads，正式）**：轻量级线程，由 JVM 管理而非 OS，每个虚拟线程占用仅几百字节栈内存，可轻松创建百万级。通过 Thread.startVirtualThread() 或 Executors.newVirtualThreadPerTaskExecutor() 创建。与传统线程 API 完全兼容，现有代码几乎零修改。阻塞操作时自动卸载（mount/unmount），不浪费载体线程。彻底变革 Java 并发编程模型，不再需要异步响应式的复杂性。\n- **结构化并发（Structured Concurrency，预览）**：将多个相关线程任务视为一个逻辑工作单元。使用 StructuredTaskScope 管理子任务生命周期，子任务失败可自动取消其他子任务，避免线程泄漏。scope.fork() 提交子任务，scope.join() 等待全部完成。支持 ShutdownOnFailure 和 ShutdownOnSuccess 两种关闭策略。\n- **Scoped Values（作用域值，预览）**：替代 ThreadLocal 的现代方案。不可变、继承安全、线程数无限扩展。ScopedValue<String> USER = ScopedValue.newInstance(); 通过 ScopedValue.where(USER, \"alice\").run(() -> {...}) 绑定值。仅在作用域内可读，退出作用域自动清除，无内存泄漏。\n- **记录模式（Record Patterns，正式）**：解构 Record，if (obj instanceof Point(int x, int y)) { return x + y; } 直接提取字段。可与 switch 模式匹配结合，嵌套解构。\n- **未命名模式和变量（Unnamed Patterns and Variables，正式）**：用 _ 表示不关心的变量或模式，catch (Exception _) { }、if (obj instanceof Point(int x, _))。\n\n**Java 22 增量特性**\n- **未命名类和实例 main 方法（预览）**：简化入门代码，不需要 class 声明和 public static void main，直接写 void main() { ... }。\n- **Stream Gatherers（预览）**：自定义 Stream 中间操作，扩展 Stream API 能力。\n- **Class-File API（预览）**：官方标准字节码操作 API，替代 ASM 等第三方库。\n- **结构化并发和 Scoped Values 继续完善。**\n\n**版本迁移注意事项**\n- Java 8 → 17：模块系统（JPMS）、移除 EE 模块（javax.* → jakarta.*）、GC 默认改为 G1、字符串存储从 char[] 改为 byte[]、移除 Sun.misc.Unsafe 部分 API。\n- Java 17 → 21：虚拟线程是最大亮点，现有代码评估使用虚拟线程替代线程池可大幅提升吞吐量。检查同步瓶颈（synchronized 块过久会 pin 住载体线程），改用 ReentrantLock 或减少持有时间。",
    "example": "// === Record 示例 ===\npublic record Order(Long id, String customerId, BigDecimal amount,\n                    LocalDateTime createdAt, OrderStatus status) {\n    // 紧凑构造器：添加校验（无参数列表）\n    public Order {\n        if (amount.compareTo(BigDecimal.ZERO) < 0) {\n            throw new IllegalArgumentException(\"金额不能为负\");\n        }\n        if (createdAt == null) createdAt = LocalDateTime.now();\n        if (status == null) status = OrderStatus.CREATED;\n    }\n    // 自定义方法\n    public boolean isCancellable() {\n        return status == OrderStatus.CREATED || status == OrderStatus.PENDING;\n    }\n}\n// 自动生成：全参构造器、getter（无 get 前缀：id()、amount()）、\n// equals/hashCode（按字段值比较）、toString\nOrder order = new Order(1L, \"C001\", new BigDecimal(\"99.99\"), null, null);\nSystem.out.println(order.id());  // 1\nSystem.out.println(order.equals(\n    new Order(1L, \"C001\", new BigDecimal(\"99.99\"), order.createdAt(), order.status())));  // true\n\n// === Sealed Class 密封类 ===\npublic sealed interface Shape permits Circle, Rectangle, Triangle {\n    double area();\n}\n// 子类必须声明为 final、sealed 或 non-sealed\npublic final class Circle implements Shape {\n    private final double r;\n    public Circle(double r) { this.r = r; }\n    @Override public double area() { return Math.PI * r * r; }\n}\npublic final class Rectangle implements Shape {\n    private final double w, h;\n    public Rectangle(double w, double h) { this.w = w; this.h = h; }\n    @Override public double area() { return w * h; }\n}\npublic non-sealed class Triangle implements Shape {\n    // non-sealed：允许任意未知子类继承 Triangle\n    private final double a, b, c;\n    public Triangle(double a, double b, double c) { this.a=a; this.b=b; this.c=c; }\n    @Override public double area() { /* 海伦公式 */ return 0; }\n}\n\n// === Switch 模式匹配（Java 21 正式）===\nstatic String format(Object o) {\n    return switch (o) {\n        case Integer i -> \"int: \" + i;\n        case Long l -> \"long: \" + l;\n        case String s when s.length() > 5 -> \"long str: \" + s;  // 守卫模式\n        case String s -> \"str: \" + s;\n        case null -> \"null value\";  // 处理 null，不再 NPE\n        default -> \"other: \" + o;\n    };\n}\n// Switch 搭配 sealed 可穷尽检查，不需要 default\ndouble areaSwitch(Shape s) {\n    return switch (s) {\n        case Circle c -> Math.PI * c.r() * c.r();\n        case Rectangle r -> r.w() * r.h();\n        case Triangle t -> t.area();\n    };  // 编译器验证所有子类已覆盖，无需 default\n}\n\n// === 虚拟线程（Java 21 正式）===\n// 方式1：直接创建\nThread vThread = Thread.startVirtualThread(() -> {\n    System.out.println(\"Hello from virtual thread: \" + Thread.currentThread());\n    // 阻塞 IO 会自动卸载，不浪费 OS 线程\n    try { Thread.sleep(Duration.ofSeconds(1)); } catch (Exception e) {}\n});\n// 方式2：ExecutorService（每个任务一个虚拟线程）\ntry (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\n    // 提交 10000 个任务完全没问题，不会像平台线程那样 OOM\n    List<Future<String>> futures = IntStream.range(0, 10_000)\n        .mapToObj(i -> executor.submit(() -> {\n            Thread.sleep(Duration.ofMillis(100));  // 模拟 IO\n            return \"Task-\" + i;\n        })).toList();\n    for (Future<String> f : futures) { f.get(); }  // 获取结果\n}\n// 方式3：Thread.ofVirtual() 构建器\nThread vt = Thread.ofVirtual().name(\"my-vt-\", 0).start(() -> {});\n\n// === 结构化并发（Java 21 预览）===\n// 需要加 --add-modules jdk.incubator.concurrent\nUserAndOrders fetchUserAndOrders(String userId) throws ExecutionException, InterruptedException {\n    try (var scope = new StructuredTaskScope.ShutdownOnFailure()) {\n        // fork 两个子任务并行执行\n        Future<User> userFuture = scope.fork(() -> userRepo.findById(userId));\n        Future<List<Order>> ordersFuture = scope.fork(() -> orderRepo.findByUserId(userId));\n        // 等待所有子任务完成\n        scope.join();\n        // 任一失败抛异常，自动取消其他子任务\n        scope.throwIfFailed();\n        // 获取结果\n        return new UserAndOrders(userFuture.resultNow(), ordersFuture.resultNow());\n    }\n}\n\n// === Scoped Values（Java 21 预览）===\n// 替代 ThreadLocal，不可变、安全、无泄漏\nstatic final ScopedValue<String> CURRENT_USER = ScopedValue.newInstance();\nstatic final ScopedValue<String> REQUEST_ID = ScopedValue.newInstance();\n\nvoid handleRequest(String userId, String reqId) {\n    // 绑定值到作用域\n    ScopedValue.where(CURRENT_USER, userId)\n               .where(REQUEST_ID, reqId)\n               .run(() -> {\n                   // 作用域内任意地方可读\n                   processBusiness();\n                   nestedCall();\n               });\n    // 退出作用域后自动清除，无需手动 remove\n}\nvoid processBusiness() {\n    // 直接读取，不需要传参\n    String user = CURRENT_USER.get();\n    String reqId = REQUEST_ID.get();\n    log.info(\"[{}] 处理用户: {} 的业务\", reqId, user);\n}\n\n// === 记录模式解构（Java 21）===\nint sumPoint(Object obj) {\n    if (obj instanceof Point(int x, int y)) {  // 直接解构 Record 字段\n        return x + y;\n    }\n    return 0;\n}\n// 嵌套解构\nrecord Point(int x, int y) {}\nrecord Rectangle(Point topLeft, Point bottomRight) {}\nint rectangleWidth(Object obj) {\n    if (obj instanceof Rectangle(Point(int x1, _), Point(int x2, _))) {\n        return x2 - x1;\n    }\n    return 0;\n}"
  },
  {
    "id": "java-ext5-springboot3",
    "title": "Spring Boot 3.x + Spring 6 深度实践",
    "level": "高级",
    "content": "**Spring Boot 3.x 核心变化**\n- **基线提升**：要求 Java 17+，Jakarta EE 9+（javax.* → jakarta.* 命名空间迁移，如 javax.servlet → jakarta.servlet）。Spring Framework 6.0+。\n- **AOT 编译与 GraalVM 原生镜像**：Ahead-Of-Time 编译，在构建时生成静态字节码，替代运行时反射和动态代理。Native Image 启动时间从秒级降到毫秒级，内存占用大幅降低，适合 Serverless、短生命周期任务。通过 native-maven-plugin 构建。注意：反射、动态代理、资源文件需在 reflect-config.json、proxy-config.json、resource-config.json 中显式声明，或使用 Spring 的 Runtime Hints 机制。\n- **ProblemDetails 统一错误响应**：实现 RFC 7807（Problem Details for HTTP APIs）标准。ErrorResponse 接口返回结构化 JSON：包含 type、title、status、detail、instance 字段。@ControllerAdvice + @ExceptionHandler 默认封装为 ProblemDetails。无需自定义统一响应体类，框架标准化。\n- **RestClient：现代 HTTP 客户端**：替代 RestTemplate 的可阻塞 HTTP 客户端，fluent API 风格，同步调用。与 WebClient（响应式）API 类似，基于 HttpMessageConverter。支持拦截器、错误处理、URI 模板。适合微服务间同步调用，比 RestTemplate 更易用。\n- **HTTP Interface 声明式 HTTP 客户端**：类似 OpenFeign，Spring 6 官方内置。定义接口 + @GetExchange/@PostExchange 注解，自动生成实现。支持路径变量、请求参数、请求体、请求头。通过 HttpServiceProxyFactory 创建代理。无需第三方依赖，与 Spring 生态无缝集成。\n\n**Micrometer 观测（Observability）**\n- **统一可观测 API**：Spring Boot 3.x 将 Micrometer 作为一等公民，集成 Metrics（指标）、Tracing（链路追踪）、Logging（日志）三位一体。\n- **Metrics**：Counter（计数器）、Timer（计时器）、Gauge（仪表盘）、DistributionSummary。默认接 Prometheus（actuator/prometheus 端点暴露）。@Observed 注解 + @Aspect 自动包装方法。\n- **Tracing**：Micrometer Tracing 统一 Brave（Zipkin）和 OpenTelemetry 两种实现。自动注入 TraceId/SpanId 到日志（MDC）。通过 management.tracing.sampling.probability=1.0 配置采样率。Feign/RestClient/Kafka 自动传播 TraceId。\n- **Logging**：日志格式默认包含 traceId、spanId，配合 ELK/Loki 可快速过滤单条请求全链路日志。\n\n**Spring 6 其他重要特性**\n- **移除旧技术**：不再支持 FreeMarker/JSP 模板（推荐 Thymeleaf）、EhCache 2、Hibernate 5、Java EE。\n- **@ConstructorBinding 检测改进**：@ConfigurationProperties 单构造器时无需显式标注 @ConstructorBinding。\n- **Spring Security 6**：Spring Security 配置类改用 Lambda DSL，WebSecurityConfigurerAdapter 已彻底移除，使用 SecurityFilterChain Bean 配置。\n- **YAML 支持变量替换**：application.yml 中可用 \"${other.key:default}\" 引用其他配置。\n\n**GraalVM Native Image 生产化实践**\n- 构建命令：mvn -Pnative native:compile 生成二进制可执行文件。\n- 启动速度：<100ms，传统 JVM 启动 2-10s。\n- 内存：RSS ~50MB vs 传统 JVM ~300MB+。\n- 局限：构建慢（分钟级）、不支持动态类加载/字节码生成、反射需提前配置、JIT 预热无（峰值性能略低 10-30%）。\n- 适合：Serverless 函数、CLI 工具、K8s 快速扩容场景。\n- 不适合：长时间运行、追求极致峰值性能、大量动态代理。",
    "example": "// === ProblemDetails 统一错误响应 ===\n@RestController\n@RequestMapping(\"/api/users\")\nclass UserController {\n    @GetMapping(\"/{id}\")\n    public User getUser(@PathVariable Long id) {\n        return userRepo.findById(id)\n            .orElseThrow(() -> new UserNotFoundException(id));\n    }\n}\n// 自定义业务异常实现 ErrorResponse\nclass UserNotFoundException extends RuntimeException implements ErrorResponse {\n    private final Long userId;\n    public UserNotFoundException(Long userId) {\n        this.userId = userId;\n    }\n    @Override\n    public ProblemDetail getBody() {\n        ProblemDetail pd = ProblemDetail.forStatusAndDetail(\n            HttpStatus.NOT_FOUND, \"用户不存在: \" + userId);\n        pd.setTitle(\"用户资源未找到\");\n        pd.setType(URI.create(\"/errors/user-not-found\"));\n        pd.setProperty(\"userId\", userId);  // 自定义扩展字段\n        return pd;\n    }\n    @Override\n    public HttpStatusCode getStatusCode() { return HttpStatus.NOT_FOUND; }\n}\n// 响应（RFC 7807 标准）:\n// {\n//   \"type\": \"/errors/user-not-found\",\n//   \"title\": \"用户资源未找到\",\n//   \"status\": 404,\n//   \"detail\": \"用户不存在: 999\",\n//   \"instance\": \"/api/users/999\",\n//   \"userId\": 999\n// }\n\n// === RestClient 现代 HTTP 客户端 ===\n@Configuration\nclass RestClientConfig {\n    @Bean\n    public RestClient restClient(RestClient.Builder builder) {\n        return builder\n            .baseUrl(\"https://api.example.com\")\n            .defaultHeader(HttpHeaders.CONTENT_TYPE, \"application/json\")\n            .requestFactory(new JdkClientHttpRequestFactory())  // Java 11 HttpClient\n            .build();\n    }\n}\n@Service\nclass ProductService {\n    private final RestClient restClient;\n    public ProductService(RestClient restClient) { this.restClient = restClient; }\n\n    public ProductDTO getProduct(Long id) {\n        return restClient.get()\n            .uri(\"/products/{id}\", id)\n            .retrieve()\n            .onStatus(HttpStatusCode::is4xxClientError,\n                (req, res) -> { throw new ProductException(\"请求错误\"); })\n            .body(ProductDTO.class);\n    }\n\n    public ProductDTO createProduct(ProductCreateDTO dto) {\n        return restClient.post()\n            .uri(\"/products\")\n            .body(dto)\n            .retrieve()\n            .body(ProductDTO.class);\n    }\n}\n\n// === HTTP Interface 声明式客户端 ===\n// 1. 定义接口\n@HttpExchange(\"/api/orders\")\ninterface OrderClient {\n    @GetExchange(\"/{id}\")\n    OrderDTO getOrder(@PathVariable Long id);\n\n    @GetExchange\n    List<OrderDTO> listOrders(@RequestParam String status);\n\n    @PostExchange\n    OrderDTO createOrder(@RequestBody OrderCreateDTO dto);\n\n    @DeleteExchange(\"/{id}\")\n    void cancelOrder(@PathVariable Long id);\n}\n// 2. 配置代理 Bean\n@Configuration\nclass HttpInterfaceConfig {\n    @Bean\n    public OrderClient orderClient(RestClient.Builder builder) {\n        RestClient restClient = builder.baseUrl(\"http://order-service\").build();\n        HttpServiceProxyFactory factory = HttpServiceProxyFactory.builder(\n            RestClientAdapter.create(restClient)).build();\n        return factory.createClient(OrderClient.class);\n    }\n}\n// 3. 直接注入使用\n@Service\nclass BusinessService {\n    @Autowired private OrderClient orderClient;  // 像调用本地方法一样\n    public void process() {\n        OrderDTO order = orderClient.getOrder(100L);\n        // ...\n    }\n}\n\n// === Micrometer 观测 ===\n// 1. 添加依赖: micrometer-registry-prometheus, micrometer-tracing-bridge-otel\n// 2. application.yml:\n// management:\n//   endpoints.web.exposure.include: health,info,prometheus\n//   tracing:\n//     sampling.probability: 1.0  # 100% 采样\n// 3. 自动观测方法\n@Observed(name = \"user.get\",\n          contextualName = \"获取用户\",\n          lowCardinalityKeyValues = {\"layer\", \"service\"})\npublic User getUser(Long id) { /* ... */ }\n// 4. 手动指标\n@Service\nclass MetricsService {\n    private final MeterRegistry meterRegistry;\n    private final Counter orderCounter;\n    private final Timer orderTimer;\n    public MetricsService(MeterRegistry registry) {\n        this.meterRegistry = registry;\n        this.orderCounter = Counter.builder(\"orders.created\")\n            .description(\"订单创建数\").tag(\"biz\", \"order\").register(registry);\n        this.orderTimer = Timer.builder(\"orders.process.duration\")\n            .description(\"订单处理耗时\").register(registry);\n    }\n    public void createOrder() {\n        orderCounter.increment();  // 计数+1\n        orderTimer.record(() -> {\n            // 被计时的业务逻辑\n            doCreateOrder();\n        });\n        // 也可以用 Sample\n        Timer.Sample sample = Timer.start(meterRegistry);\n        try { doCreateOrder(); }\n        finally { sample.stop(orderTimer); }\n    }\n}\n\n// === GraalVM Native 反射 Hints ===\n@Configuration\n@ImportRuntimeHints(NativeConfig.MyHints.class)\nclass NativeConfig {\n    static class MyHints implements RuntimeHintsConfigurer {\n        @Override\n        public void configureRuntimeHints(RuntimeHints hints, ClassLoader cl) {\n            // 声明反射\n            hints.reflection().registerType(MyDTO.class,\n                MemberCategory.values());\n            // 声明资源文件\n            hints.resources().registerPattern(\"templates/*.html\");\n            // 声明 JDK 动态代理\n            hints.proxies().registerJdkProxy(MyService.class);\n        }\n    }\n}\n// pom.xml native profile:\n// <plugin>\n//   <groupId>org.graalvm.buildtools</groupId>\n//   <artifactId>native-maven-plugin</artifactId>\n// </plugin>\n// 构建: mvn -Pnative native:compile\n// 产物: target/myapp (二进制可执行文件)"
  },
  {
    "id": "java-ext5-cloudnative",
    "title": "云原生 Java 实践",
    "level": "高级",
    "content": "**云原生 Java 核心概念**\n- 十二要素应用（The Twelve-Factor App）：代码库单一、依赖显式声明、配置存环境、外部资源（数据库等）可替换、构建/发布/运行分离、无状态进程、端口暴露、多实例水平扩展、快速启动优雅停机、开发/生产环境一致、日志视为事件流、管理后台作为一次性进程。\n- 不可变基础设施：容器镜像构建后不变，通过滚动更新替代修改运行中实例。\n\n**GraalVM 原生镜像与容器化**\n- 原生镜像（Native Image）优势：启动<100ms，内存<100MB，适合 Serverless 和快速扩容。\n- **Docker 多阶段构建优化**：第一阶段用带 GraalVM 的 Maven 镜像编译 Java 源码并 native:compile；第二阶段用极精简的基础镜像（如 distroless/java17-debian11 或 gcr.io/distroless/base）复制二进制，镜像体积从 JDK 镜像的 600MB+ 降至 100MB 以内。\n- **分层镜像（Layer Caching）**：Dockerfile 按变化频率排序指令，先复制 pom.xml 下载依赖（缓存层），再复制源码编译。利用 Docker layer cache 加快重复构建。Spring Boot 3.x 支持分层 JAR：层分为 dependencies（依赖，最不常变）、spring-boot-loader、snapshot-dependencies、application（应用代码，最常变）。通过 layertools 模式提取每层，每层独立镜像层缓存。\n- **JVM 容器感知**：Java 10+ 自动识别容器 CPU/Memory 限制（-XX:+UseContainerSupport）。JVM 堆内存默认：容器内存限制的 1/4（MaxRAMPercentage=25）。可通过 JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75.0 调整。避免容器内存 limit 设 1GB 但 JVM 用 2GB 导致 OOM Kill。\n\n**Kubernetes 探针与健康检查**\n- **Liveness Probe（存活探针）**：判断容器是否运行中（死锁、卡死）。失败则 kubelet 重启容器。Spring Boot Actuator: /actuator/health/liveness（内部状态）。\n- **Readiness Probe（就绪探针）**：判断容器是否可接收流量。失败则 Service 摘流。启动慢的应用避免流量打到未就绪实例。Spring Boot Actuator: /actuator/health/readiness。\n- **Startup Probe（启动探针）**：判断容器是否启动完成。未通过时暂停存活/就绪探针。慢启动应用防止启动中被误杀。可给较长时间（如 60 秒 * 30 次 = 30 分钟）。\n- 探针类型：exec（执行命令 exit 0=健康）、httpGet（HTTP 2xx/3xx=健康）、tcpSocket（端口可连=健康）。\n- Spring Boot 健康状态：Cron 线程池耗尽、DB 连接失败、磁盘不足等会自动降级。通过 HealthIndicator SPI 扩展自定义检查。\n- **优雅停机**：SIGTERM 信号 → Spring Boot 30s 内完成请求处理、停止任务、关闭线程池 → 退出。server.shutdown=graceful，生命周期 liveness 探针配置 preStop 钩子。\n\n**Helm 部署 Spring Boot 应用**\n- Helm 是 Kubernetes 包管理器，相当于 Linux 的 apt/yum。Chart 是模板包，包含 Kubernetes 资源模板。\n- 典型 Chart 结构：Chart.yaml（元信息）、values.yaml（默认值，可覆盖）、templates/（Deployment、Service、Ingress、ConfigMap 等模板）。\n- 模板语言：Go template + Sprig 函数。{{ .Values.replicaCount }} 引用 values.yaml 值。\n- Release 是 Chart 安装后的实例。helm install my-app ./my-chart -f prod-values.yaml 部署。helm upgrade/rollback 版本管理。\n- Spring Cloud Kubernetes：让 Spring 应用与 K8s 原生集成。@RefreshScope 自动从 ConfigMap/Secret 热刷新配置（无需重启）。KubernetesClient 访问 K8s API。Kubernetes Discovery Client 服务发现（通过 Service Endpoints）。\n\n**生产化最佳实践**\n- **资源 Requests/Limits**：Requests 保证调度（K8s 选有足够资源的节点），Limits 限制上限防止占用。JVM 堆内存要与容器 Limits 配合（留空间给元空间、线程栈、堆外内存）。\n- **HPA（Horizontal Pod Autoscaler）**：基于 CPU 利用率、自定义指标（如 QPS、队列长度）自动扩缩副本数。Spring Boot Actuator 暴露自定义指标给 Prometheus，配合 Prometheus Adapter 供 HPA 使用。\n- **ConfigMap/Secret 管理配置**：不要把配置打进镜像。环境变量敏感信息用 Secret（base64），非敏感用 ConfigMap。挂载为文件或环境变量。\n- **Sidecar 模式**：日志采集（Filebeat/Fluentd）、代理（Envoy Service Mesh）、认证代理（Oauth2-Proxy）作为 Sidecar 容器共享 Pod。\n- **PodDisruptionBudget**：保证节点维护、升级时最少可用副本数，防止服务中断。",
    "example": "// === Dockerfile 优化：多阶段 + 分层 JAR ===\n// 阶段1：构建（含 GraalVM Maven 镜像）\n'''\nFROM ghcr.io/graalvm/graalvm-ce:ol8-java17-22 AS builder\nWORKDIR /app\n# 先复制 pom.xml，利用 Docker 缓存（pom 不变时不重新下载依赖）\nCOPY pom.xml .\nRUN ./mvnw -B dependency:go-offline\n# 再复制源码\nCOPY src ./src\n# 构建原生镜像\nRUN ./mvnw -B -Pnative native:compile -DskipTests\n\n# 阶段2：运行（精简 distroless 基础镜像，~20MB 基础）\nFROM gcr.io/distroless/base-debian11\nWORKDIR /app\nCOPY --from=builder /app/target/myapp ./myapp\nEXPOSE 8080\nUSER nonroot\nENTRYPOINT [\"./myapp\"]\n'''\n\n// === Dockerfile：JVM 模式分层 JAR ===\n'''\nFROM eclipse-temurin:17-jre AS builder\nWORKDIR /app\nCOPY target/*.jar app.jar\n# 提取 Spring Boot 分层 JAR\nRUN java -Djarmode=layertools -jar app.jar extract\n\nFROM eclipse-temurin:17-jre\nWORKDIR /app\n# 按层复制，最不常变的层先复制（最大缓存命中率）\nCOPY --from=builder /app/dependencies/ ./\nCOPY --from=builder /app/spring-boot-loader/ ./\nCOPY --from=builder /app/snapshot-dependencies/ ./\nCOPY --from=builder /app/application/ ./\n# JVM 容器感知参数\nENV JAVA_TOOL_OPTIONS=\"-XX:MaxRAMPercentage=75.0 -XX:+UseG1GC\"\nEXPOSE 8080\nENTRYPOINT [\"java\", \"org.springframework.boot.loader.launch.JarLauncher\"]\n'''\n\n// === K8s Deployment 探针配置 ===\n'''\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: order-service\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: order-service\n  template:\n    metadata:\n      labels:\n        app: order-service\n    spec:\n      containers:\n      - name: app\n        image: registry.example.com/order-service:v1.2.0\n        ports:\n        - containerPort: 8080\n        # 资源约束\n        resources:\n          requests:\n            cpu: \"500m\"      # 调度需要 0.5 核\n            memory: \"512Mi\"  # 调度需要 512MB\n          limits:\n            cpu: \"2\"         # 最多用 2 核\n            memory: \"1Gi\"    # 超过被 OOM Kill\n        # 启动探针（慢启动保护）\n        startupProbe:\n          httpGet:\n            path: /actuator/health/readiness\n            port: 8080\n          failureThreshold: 30   # 最多等 30*10s = 5 分钟\n          periodSeconds: 10\n        # 存活探针（卡死自动重启）\n        livenessProbe:\n          httpGet:\n            path: /actuator/health/liveness\n            port: 8080\n          initialDelaySeconds: 30\n          periodSeconds: 15\n          timeoutSeconds: 3\n        # 就绪探针（未就绪不接流量）\n        readinessProbe:\n          httpGet:\n            path: /actuator/health/readiness\n            port: 8080\n          initialDelaySeconds: 10\n          periodSeconds: 5\n          timeoutSeconds: 2\n        # 优雅停机 preStop（先摘流再等 Spring 处理完）\n        lifecycle:\n          preStop:\n            exec:\n              command: [\"sh\", \"-c\", \"sleep 15\"]\n        # 环境变量来自 ConfigMap/Secret\n        env:\n        - name: SPRING_PROFILES_ACTIVE\n          value: \"prod\"\n        - name: DB_PASSWORD\n          valueFrom:\n            secretKeyRef:\n              name: db-secret\n              key: password\n        - name: REDIS_HOST\n          valueFrom:\n            configMapKeyRef:\n              name: app-config\n              key: redis.host\n      terminationGracePeriodSeconds: 60  # 60s 优雅停机宽限期\n'''\n\n// === Spring Boot 自定义 HealthIndicator ===\n@Component\npublic class RedisHealthIndicator extends AbstractHealthIndicator {\n    private final RedisTemplate<String, String> redisTemplate;\n    @Override\n    protected void doHealthCheck(Health.Builder builder) throws Exception {\n        try {\n            String pong = redisTemplate.execute(\n                (RedisCallback<String>) conn -> {\n                    return new String(conn.ping());\n                });\n            if (\"PONG\".equals(pong)) {\n                builder.up().withDetail(\"ping\", \"ok\");\n            } else {\n                builder.down().withDetail(\"ping\", pong);\n            }\n        } catch (Exception e) {\n            builder.down(e).withDetail(\"error\", e.getMessage());\n        }\n    }\n}\n\n// === HPA 基于 CPU 自动扩缩 ===\n'''\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: order-service-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: order-service\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n  - type: Resource\n    resource:\n      name: cpu\n      target:\n        type: Utilization\n        averageUtilization: 70  # CPU 超 70% 扩容\n  - type: Pods\n    pods:\n      metric:\n        name: http_requests_per_second  # 自定义 Prometheus 指标\n      target:\n        type: AverageValue\n        averageValue: \"500\"  # 每 Pod QPS > 500 扩容\n  behavior:\n    scaleUp:\n      stabilizationWindowSeconds: 30   # 扩容稳定窗口 30s\n    scaleDown:\n      stabilizationWindowSeconds: 300  # 缩容稳定窗口 5min（防抖动）\n'''\n\n// === Spring Cloud Kubernetes 配置热刷新 ===\n// 添加依赖 spring-cloud-starter-kubernetes-client-config\n// bootstrap.yml:\n// spring:\n//   cloud:\n//     kubernetes:\n//       reload:\n//         enabled: true        # ConfigMap 变化自动刷新\n//         mode: event          # 监听事件（轮询或事件）\n//         strategy: refresh    # refresh 或 restart_context\n@RestController\n@RefreshScope  // 这个 Bean 会在配置变更时重新创建\nclass ConfigController {\n    @Value(\"${app.feature.flag:false}\")\n    private boolean featureFlag;\n    @GetMapping(\"/flag\")\n    public boolean getFlag() { return featureFlag; }\n}\n// 修改 ConfigMap: kubectl edit cm app-config → 无需重启自动生效"
  },
  {
    "id": "java-ext5-observability",
    "title": "Java 可观测性与生产排查",
    "level": "高级",
    "content": "**可观测性三大支柱**\n- **Metrics（指标）**：可聚合的数值型时间序列数据（CPU 使用率、请求 QPS、P99 延迟、内存占用）。特点：可聚合、可计算、可告警（阈值触发）。存储：Prometheus、InfluxDB、M3DB。\n- **Logging（日志）**：离散的、带时间戳的事件文本。特点：细节丰富、可回溯单条请求详情。存储：ELK (Elasticsearch/Logstash/Kibana)、Loki + Grafana。\n- **Tracing（链路追踪）**：单次请求在分布式系统中的调用链。特点：跨服务调用关系、各阶段耗时、瓶颈定位。存储：Jaeger、Zipkin、Tempo。\n\n**Micrometer + Prometheus + Grafana 指标体系**\n- **Micrometer 是 Spring Boot 3.x 默认指标门面**（类似 SLF4J 之于日志）。统一 API 对接多个监控后端（Prometheus、Graphite、Datadog、CloudWatch）。\n- **Meter（仪表）类型**：Counter（单调递增计数器，如请求总数）、Timer（计时器，含次数+总耗时+分位数）、Gauge（当前值，可升可降，如在线用户数、线程池活跃数）、DistributionSummary（分布，如请求体大小）、LongTaskTimer（长任务计时器，任务执行中就记录）。\n- **Prometheus 拉模型**：Prometheus 定时 scrape 应用 /actuator/prometheus 端点（端口 8080，也可独立 9090 端口）。避免应用 push 到监控系统（防火墙、端口暴露问题）。标签（Label）用于多维度聚合（by status、by instance、by endpoint）。\n- **Grafana 可视化**：Prometheus 作为数据源，编写 PromQL 查询。常用图表：QPS（rate(http_requests_seconds_count[5m])）、P99 延迟（histogram_quantile(0.99, rate(http_requests_seconds_bucket[5m]))）、错误率（status=5xx）。\n- **告警（Alerting）**：Prometheus Alertmanager 配置告警规则（错误率 >5% 持续 5min），支持邮件、钉钉、飞书、PagerDuty 多渠道。\n\n**OpenTelemetry 全链路观测**\n- **OpenTelemetry（OTel）是 CNCF 顶级项目，统一 Tracing + Metrics + Logs 采集标准**，终结 OpenTracing、OpenCensus 分裂局面。替代 Zipkin Brave、Spring Cloud Sleuth。\n- **核心概念**：Trace（一条请求完整链路）、Span（链路中的一个操作，带开始结束时间、属性）、Context（TraceId/SpanId 传播载体）、Baggage（跨 Span 透传业务数据，如 userId）。\n- **埋点方式**：1）自动埋点（Java Agent 无侵入，java -javaagent:opentelemetry-javaagent.jar -jar app.jar，自动拦截 Spring MVC、JPA、Kafka、Redis、HTTP Client 等）；2）手动埋点（Tracer API @WithSpan 注解）。\n- **传播协议**：W3C TraceContext（默认，traceparent/tracestate HTTP 头）、B3（Zipkin 格式）。跨语言/跨厂商标准。\n- **采样策略**：Head-based（入站时决定是否采样，简单但可能错过慢请求）、Tail-based（全量收集后端再抽样，存 1% 错误+慢请求，成本高）。\n- **Jaeger UI 查询**：按 TraceId/SpanId 精确搜、按服务/接口/耗时范围搜索、火焰图可视化（看哪个 Span 耗时最久）、对比异常请求与正常请求 Span 差异。\n\n**Arthas 阿里生产诊断神器**\n- 无需重启 Java 进程，attach 到运行中 JVM。命令式交互，不阻塞业务。\n- **thread 命令**：thread 显示线程 CPU 占用排行；thread -n 3 显示前3高 CPU 线程堆栈；thread -b 找出阻塞其他线程的线程；thread 状态分析（BLOCKED/WAITING）。\n- **dashboard 面板**：实时线程、内存（Eden/Survivor/Old/Metaspace）、GC 次数耗时、系统负载，一眼看全貌。\n- **jad 反编译**：jad com.foo.BarService 反编译线上运行的类，确认代码版本是否是最新（避免发布旧代码）。\n- **watch 方法监控**：watch com.foo.BarService methodName '{params, returnObj, throwExp}' -x 3 -n 5 '#cost>100' 观察方法入参、返回值、异常，耗时超过 100ms 才输出，输出 5 次停止，深度展开 3 层。\n- **trace 调用链耗时**：trace com.foo.BarService process 追踪方法调用链，输出每个子方法耗时，找到性能瓶颈（哪个方法慢）。支持 -j 跳过 JDK 方法。\n- **profiler 火焰图**：profiler start -e cpu/wall 开始采样；sleep 60 让业务跑一会；profiler stop 生成火焰图。Cpu 模式看 CPU 热点，Wall 模式看慢 IO 阻塞。\n- **heapdump + jfr**：heapdump /tmp/heap.hprof 导出堆（类似 jmap）；jfr 录制 JDK Flight Recorder 事件（更全的性能数据）。\n\n**GC 日志分析实战**\n- **GC 日志开启**：JDK 9+ 统一日志：-Xlog:gc*=info:file=gc.log:time,level,tags:filecount=10,filesize=100m（滚动）。JDK 8：-XX:+PrintGCDetails -XX:+PrintGCTimeStamps -XX:+PrintGCDateStamps -Xloggc:gc.log。\n- **G1 GC 日志关键字段**：Pause Young (Normal) (G1 Evacuation Pause) 年轻代回收；Pause Initial Mark / Remark / Cleanup 并发周期；Humongous Allocation 超大对象分配（>50% Region 直接进老年代，隐患）。\n- **分析工具**：GCEasy（在线 gceasy.io，上传自动生成报告）、G1GC Log Analyzer、GCViewer。指标关注：YGC 频率（每秒 <1 次好）、YGC 耗时（<50ms 好）、FGC 次数（几乎没有好）、Full GC 后老年代回收比例（<80% 有内存泄漏嫌疑）。\n- **常见 GC 问题**：1）YGC 频繁 → 年轻代太小或对象分配过多；2）FGC 频繁 → 老年代满、System.gc() 调用、大对象直接晋升；3）YGC 停顿长 → 大对象扫描、旧 Regions 过多、增大 -XX:MaxGCPauseMillis 或调大堆。\n- **生产 GC 参数模板**：-XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UnlockExperimentalVMOptions -XX:+UseCGroupMemoryLimitForHeap -XX:+ParallelRefProcEnabled -XX:+PerfDisableSharedMem。",
    "example": "// === Micrometer 自定义指标 ===\n@Configuration\nclass MetricsConfig {\n    // 注册一个全局 Gauge 监控线程池活跃数\n    @Bean\n    public ThreadPoolExecutorMetrics threadPoolMetrics(ThreadPoolExecutor bizPool) {\n        return new ThreadPoolExecutorMetrics(bizPool);\n    }\n}\n\n@Component\nclass OrderServiceMetrics {\n    private final Counter orderCreatedCounter;\n    private final Counter orderFailedCounter;\n    private final Timer orderProcessTimer;\n    private final AtomicInteger onlineUsers = new AtomicInteger();\n\n    public OrderServiceMetrics(MeterRegistry registry) {\n        // 订单创建数（带标签区分渠道）\n        this.orderCreatedCounter = Counter.builder(\"orders.created.total\")\n            .description(\"订单创建总数\")\n            .tag(\"biz\", \"order\")\n            .register(registry);\n        // 订单失败数\n        this.orderFailedCounter = Counter.builder(\"orders.failed.total\")\n            .tag(\"reason\", \"stock_out\")  // 库存不足标签\n            .register(registry);\n        // 订单处理耗时 Timer（自带 count + sum + bucket）\n        this.orderProcessTimer = Timer.builder(\"orders.process.duration\")\n            .description(\"订单处理耗时\")\n            .publishPercentiles(0.5, 0.95, 0.99)  // 输出 P50/P95/P99\n            .publishPercentileHistogram()\n            .serviceLevelObjectives(Duration.ofMillis(100), Duration.ofMillis(500))\n            .register(registry);\n        // Gauge：在线用户数（Supplier 实时计算）\n        Gauge.builder(\"users.online\", onlineUsers, AtomicInteger::get)\n            .description(\"当前在线用户数\")\n            .register(registry);\n    }\n    // 在业务逻辑中调用\n    public void recordCreated(String channel) {\n        orderCreatedCounter.increment();\n        // 带动态标签（不推荐太多，Cardinality 爆炸）\n    }\n    public void recordProcess(Runnable logic) {\n        // Timer 包裹业务代码\n        orderProcessTimer.record(logic);\n    }\n    public void incrOnlineUser() { onlineUsers.incrementAndGet(); }\n    public void decrOnlineUser() { onlineUsers.decrementAndGet(); }\n}\n\n// === @Observed 注解自动观测（AOP）===\n@Configuration\n@EnableAspectJAutoProxy\nclass ObservedConfig {\n    // 需要 micrometer-observation-aop 依赖\n    @Bean\n    public ObservedAspect observedAspect(ObservationRegistry registry) {\n        return new ObservedAspect(registry);\n    }\n}\n@Service\nclass UserService {\n    @Observed(name = \"user.service\",\n              contextualName = \"查询用户详情\",\n              lowCardinalityKeyValues = {\"layer\", \"service\", \"module\", \"user\"})\n    public UserDetailDTO getUserDetail(Long userId) {\n        // 这个方法会被自动计时+计数，并创建 Span（tracing 依赖在的话）\n        return doQuery(userId);\n    }\n}\n\n// === OpenTelemetry 手动埋点 ===\n// 1. 加依赖 opentelemetry-api, opentelemetry-sdk\n// 2. 也可以直接用 Java Agent 自动埋点（推荐，零代码）\n// java -javaagent:opentelemetry-javaagent.jar \\\n//      -Dotel.service.name=order-service \\\n//      -Dotel.traces.exporter=jaeger \\\n//      -Dotel.exporter.jaeger.endpoint=http://jaeger:14250 \\\n//      -jar app.jar\n\n@Service\nclass OtelManualService {\n    private final Tracer tracer = GlobalOpenTelemetry.getTracer(\"order-service\");\n\n    public void processOrder(Long orderId) {\n        // 手动创建 Span\n        Span span = tracer.spanBuilder(\"processOrder\")\n            .setAttribute(\"order.id\", orderId)\n            .setAttribute(\"order.channel\", \"APP\")\n            .startSpan();\n        try (Scope scope = span.makeCurrent()) {  // Span 放入 Context\n            // 业务逻辑\n            validateOrder(orderId);  // 子方法会自动继承这个 Span\n            reserveStock(orderId);\n            chargePayment(orderId);\n            span.addEvent(\"订单处理完成\");  // 事件点\n        } catch (Exception e) {\n            span.recordException(e);  // 记录异常\n            span.setStatus(StatusCode.ERROR, e.getMessage());\n            throw e;\n        } finally {\n            span.end();\n        }\n    }\n    // 注解方式更简单\n    @WithSpan(\"validateOrder\")  // 自动包装为子 Span\n    void validateOrder(@SpanAttribute(\"order.id\") Long orderId) {\n        /* 校验逻辑 */\n    }\n}\n\n// === Arthas 常用命令（实战场景）===\n'''\n# 1. 连接到目标 JVM 进程\njava -jar arthas-boot.jar  # 输入序号选进程\n\n# 2. 排查 CPU 高：看哪些线程占用高并打印栈\nthread -n 5 -i 2000  # 2秒内 CPU 前5的线程，自动打印栈\n\n# 3. 排查接口慢：trace 调用链找瓶颈\ntrace com.example.service.OrderServiceImpl createOrder \\\n  '#cost > 500' -j  # 耗时>500ms的才显示，-j 跳过JDK方法\n\n# 4. 线上出参/入参调试：不打日志直接看\nwatch com.example.service.OrderServiceImpl createOrder \\\n  '{params, returnObj, throwExp}' \\  # 看参数、返回、异常\n  -x 3 \\  # 对象展开3层\n  -n 10  # 只抓10次\n\n# 5. 反编译类，确认线上代码版本\njad --source-only com.example.service.OrderServiceImpl\n\n# 6. 火焰图找 CPU/IO 瓶颈\nprofiler start -e itimer  # CPU 采样模式\nsleep 60                   # 等60秒采集\nprofiler stop              # 生成 flamegraph.html，浏览器打开看热点\nprofiler start -e wall     # 墙钟模式（含阻塞时间，适合找 IO 瓶颈）\n\n# 7. dump 堆（OOM 排查）\nheapdump --live /tmp/heap_20240101.hprof\n\n# 8. 实时监控面板\ndashboard -n 10  # 每500ms刷新10次，看内存、GC、线程\n\n# 9. 找类加载信息\nsc -d com.example.OrderService  # 查找类、类加载器、代码来源 jar\n\n# 10. 热更新代码（线上紧急修复，慎用！）\nredefine /tmp/OrderServiceImpl.class  # 直接替换已加载类的字节码\n'''\n\n// === 应用内日志加 TraceId（MDC）===\n// 依赖 micrometer-tracing，自动注入 MDC\n@Configuration\nclass LogConfig {\n    // logback-spring.xml pattern 中加 traceId:\n    // \"[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] [%X{traceId:-},%X{spanId:-}]\"\n    // \" %-5level %logger{36} - %msg%n\"\n}\n// 最终日志示例:\n// [2024-01-01 10:00:00.123] [http-nio-8080-exec-1]\n//   [4bf92f3577b34da6a3ce929d0e0e4736,00f067aa0ba902b7]\n//   INFO  c.e.service.OrderService - 创建订单成功: 12345\n// 用 traceId 在 Jaeger、ELK 中可定位整条链路所有日志"
  },
  {
    "id": "java-ext5-data-access",
    "title": "现代 Java 数据访问",
    "level": "高级",
    "content": "**Spring Data JPA 规范与实践**\n- **Spring Data 统一资源库模式**：定义 Repository 接口，自动生成实现。CrudRepository（基本 CRUD）→ PagingAndSortingRepository（分页排序）→ JpaRepository（JPA 特性如 flush、批量删除）。\n- **方法名派生查询（Query Derivation）**：findByNameAndAgeGreaterThan(String name, int age) 按方法名自动生成 JPQL 查询。关键字：And/Or/Between/LessThan/GreaterThan/IsNull/IsNotNull/Like/OrderBy/Top/First/Distinct。超过3-4个条件可读性差，改用 Specification 或 QueryDSL。\n- **@Query 自定义查询**：JPQL（面向实体）或 native SQL。:paramName 命名参数，#{#entityName} SpEL 引用实体名。支持投影（Projection）只查必要字段：interface UserInfoProjection { Long getId(); String getName(); }。\n- **Specification 动态查询**：Criteria API 封装，JpaSpecificationExecutor 接口，可组合多个 Predicate。适合前端多条件动态筛选（名称模糊、状态、日期范围等）。\n- **Entity Graph 解决 N+1**：@NamedEntityGraph + @EntityGraph 注解，一次查询 JOIN FETCH 加载关联实体，替代写死 FETCH JOIN。\n- **审计功能**：@EntityListeners(AuditingEntityListener.class) + @CreatedDate、@LastModifiedDate、@CreatedBy、@LastModifiedBy。@EnableJpaAuditing 开启。\n- **PersistenceContext**：EntityManager 的 1 级缓存、脏检查、flush 时机。\n- **Open In View 问题**：OSIV 默认开启导致懒加载在 Controller 层还能查 DB（视图层仍有 Session），长事务风险。生产建议关闭 spring.jpa.open-in-view=false，在 Service 层 JOIN FETCH 加载所需关联。\n\n**MyBatis-Plus 企业级增强**\n- **MyBatis-Plus 是 MyBatis 增强工具包（国内流行），零 SQL 单表 CRUD**。\n- **BaseMapper<T> 内置方法**：insert / deleteById / deleteBatchIds / updateById / selectById / selectBatchIds / selectOne / selectList / selectCount / selectPage，减少 90% 基础 SQL。\n- **条件构造器 Wrapper**：QueryWrapper（查删改）、LambdaQueryWrapper（类型安全，字段用方法引用避免字符串列名写错）。wrapper.like(\"name\", \"张\").ge(\"age\", 18).in(\"status\", 1,2).orderByDesc(\"create_time\")。支持链式编程。\n- **IService 业务层接口**：通用 CRUD Service 接口 + 实现（ServiceImpl）。save / saveBatch（批量插入）/ removeById / update / getById / list / page。默认批大小 1000。\n- **分页插件**：PaginationInnerInterceptor（MyBatis Plus 3.4+）。IPage<T> selectPage(IPage<T> page, Wrapper<T> queryWrapper)。自动生成 count SQL + limit 分页 SQL。\n- **逻辑删除**：@TableLogic 注解字段，删除操作自动变为 UPDATE status = deleted。查询自动过滤已删除。全局配置 mybatis-plus.global-config.db-config.logic-delete-field/logic-not-delete-value。\n- **自动填充**：MetaObjectHandler 实现 insertFill/updateFill，处理 create_time、update_time、create_by 等公共字段。\n- **乐观锁**：@Version + OptimisticLockerInnerInterceptor。update 时自动带上 version 条件，版本不匹配不更新（返回 0 受影响行数）。\n- **多租户插件**：TenantLineInnerInterceptor 自动在 SQL 追加 tenant_id 条件，SaaS 场景常用。\n- **防全表更新删除**：BlockAttackInnerInterceptor 拦截无 WHERE 的 UPDATE/DELETE，防止手滑灾难。\n\n**QueryDSL 类型安全查询**\n- QueryDSL 通过 APT 注解处理器生成 Q 类（静态查询模型），QUser.user.name.like(\"张\") 写查询代码。编译期检查字段名拼写错误，优于字符串 Criteria。\n- 集成 JPA：JPAQueryFactory selectFrom(QUser.user).where(QUser.user.age.gt(18)).fetch()。适合复杂查询。\n\n**jOOQ 流式 SQL 构建器**\n- jOOQ 直接写 Java 代码构建 SQL，接近手写 SQL 的感觉，强类型。DSLContext.select(USER.NAME, USER.AGE).from(USER).where(USER.AGE.gt(18)).orderBy(USER.ID.desc()).fetchInto(UserDTO.class)。\n- 代码生成器根据表结构生成类，数据库变更编译期发现。\n\n**Hibernate Reactive + Panache 响应式数据访问**\n- 传统 JDBC 阻塞，Reactive 用 R2DBC（非阻塞关系型数据库驱动）。\n- Hibernate Reactive 是 JPA 规范的响应式实现，返回 Uni/Mono。\n- Panache（Quarkus 生态，Spring 也可用）简化 Repository 模式：PanacheRepository 提供类似 Spring Data 的方法，返回 Uni/Multi。适合 WebFlux/Vert.x 全栈响应式应用。",
    "example": "// === Spring Data JPA：复杂多条件 Specification ===\n@Entity\n@Table(name = \"orders\")\n@EntityListeners(AuditingEntityListener.class)\npublic class Order {\n    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n    private String customerId;\n    @Enumerated(EnumType.STRING)\n    private OrderStatus status;\n    private BigDecimal amount;\n    @CreatedDate\n    private LocalDateTime createdAt;\n    @Version\n    private Long version;  // 乐观锁\n}\n\n// Repository 接口\npublic interface OrderRepository extends JpaRepository<Order, Long>,\n    JpaSpecificationExecutor<Order>  {  // 支持 Specification\n\n    // === 方法名派生查询 ===\n    List<Order> findByCustomerIdAndStatus(String customerId, OrderStatus status);\n    List<Order> findByAmountBetween(BigDecimal min, BigDecimal max);\n    List<Order> findTop10ByStatusOrderByCreatedAtDesc(OrderStatus status);\n    Page<Order> findByStatus(OrderStatus status, Pageable pageable);\n\n    // === @Query JPQL + 投影 ===\n    @Query(\"SELECT o FROM Order o WHERE o.customerId = :custId \" +\n           \"AND o.createdAt BETWEEN :start AND :end\")\n    List<Order> findInRange(@Param(\"custId\") String custId,\n                            @Param(\"start\") LocalDateTime start,\n                            @Param(\"end\") LocalDateTime end);\n\n    // 返回投影接口（只查 id + amount，提高性能）\n    interface OrderSimpleProjection {\n        Long getId();\n        BigDecimal getAmount();\n    }\n    @Query(\"SELECT o.id as id, o.amount as amount FROM Order o WHERE o.status = :st\")\n    List<OrderSimpleProjection> findSimpleByStatus(@Param(\"st\") OrderStatus status);\n}\n\n// Specification 动态条件构建（前端传任意筛选组合）\npublic class OrderSpecs {\n    public static Specification<Order> build(OrderQueryDTO query) {\n        return (root, cb, cq) -> {\n            List<Predicate> preds = new ArrayList<>();\n            if (query.getCustomerId() != null) {\n                preds.add(cb.equal(root.get(\"customerId\"), query.getCustomerId()));\n            }\n            if (query.getStatusList() != null && !query.getStatusList().isEmpty()) {\n                preds.add(root.get(\"status\").in(query.getStatusList()));\n            }\n            if (query.getMinAmount() != null) {\n                preds.add(cb.ge(root.get(\"amount\"), query.getMinAmount()));\n            }\n            if (query.getStartDate() != null) {\n                preds.add(cb.greaterThanOrEqualTo(\n                    root.get(\"createdAt\"), query.getStartDate()));\n            }\n            return cb.and(preds.toArray(new Predicate[0]));\n        };\n    }\n}\n@Service\npublic class OrderService {\n    @Autowired private OrderRepository orderRepo;\n\n    public Page<Order> search(OrderQueryDTO query, Pageable pageable) {\n        Specification<Order> spec = OrderSpecs.build(query);\n        return orderRepo.findAll(spec, pageable);\n    }\n}\n\n// === MyBatis-Plus 实战 ===\n// 1. 实体类\n@Data\n@TableName(\"t_user\")\npublic class UserEntity {\n    @TableId(type = IdType.AUTO)\n    private Long id;\n    private String name;\n    private Integer age;\n    @TableField(\"status\")  // 映射表字段\n    private Integer userStatus;\n    @TableLogic  // 逻辑删除: 1=已删, 0=正常\n    private Integer deleted;\n    @TableField(fill = FieldFill.INSERT)  // 插入时自动填充\n    private LocalDateTime createTime;\n    @TableField(fill = FieldFill.INSERT_UPDATE)  // 插入+更新自动填充\n    private LocalDateTime updateTime;\n    @Version  // 乐观锁\n    private Integer version;\n}\n\n// 2. Mapper 继承 BaseMapper（自动CRUD）\npublic interface UserMapper extends BaseMapper<UserEntity> {\n    // 自定义多表关联 SQL（XML 或注解）\n    @Select(\"SELECT u.*, d.dept_name FROM t_user u \" +\n            \"LEFT JOIN t_dept d ON u.dept_id = d.id WHERE u.id = #{id}\")\n    UserWithDeptVO selectUserWithDept(Long id);\n}\n\n// 3. 条件构造器 Lambda（类型安全，推荐）\n@Service\npublic class UserService extends ServiceImpl<UserMapper, UserEntity> {\n    // extends ServiceImpl 自带 save/saveBatch/remove/list/page 等通用方法\n\n    public List<UserEntity> searchUsers(String nameKw, Integer minAge,\n                                        Integer maxAge, List<Integer> statusList) {\n        return this.list(new LambdaQueryWrapper<UserEntity>()\n            .like(StringUtils.hasText(nameKw), UserEntity::getName, nameKw)\n            .ge(minAge != null, UserEntity::getAge, minAge)\n            .le(maxAge != null, UserEntity::getAge, maxAge)\n            .in(statusList != null && !statusList.isEmpty(),\n                UserEntity::getUserStatus, statusList)\n            .orderByDesc(UserEntity::getCreateTime)\n            .last(\"LIMIT 500\")  // 限制返回，防大数据量\n        );\n    }\n\n    public boolean updateUser(UserEntity user) {\n        // UpdateWrapper 只更新非 null 字段，或 LambdaUpdate\n        return this.update(new LambdaUpdateWrapper<UserEntity>()\n            .set(UserEntity::getName, user.getName())\n            .set(UserEntity::getAge, user.getAge())\n            .eq(UserEntity::getId, user.getId())\n            // 乐观锁自动带上 version 条件\n        );\n    }\n\n    public IPage<UserEntity> pageUsers(int current, int size, String keyword) {\n        Page<UserEntity> page = new Page<>(current, size);\n        return this.page(page, new LambdaQueryWrapper<UserEntity>()\n            .like(UserEntity::getName, keyword));\n    }\n}\n\n// 4. 自动填充处理器\n@Component\npublic class MyMetaHandler implements MetaObjectHandler {\n    @Override\n    public void insertFill(MetaObject metaObject) {\n        this.strictInsertFill(metaObject, \"createTime\",\n            LocalDateTime.class, LocalDateTime.now());\n        this.strictInsertFill(metaObject, \"updateTime\",\n            LocalDateTime.class, LocalDateTime.now());\n    }\n    @Override\n    public void updateFill(MetaObject metaObject) {\n        this.strictUpdateFill(metaObject, \"updateTime\",\n            LocalDateTime.class, LocalDateTime.now());\n    }\n}\n\n// 5. MyBatis-Plus 插件配置\n@Configuration\n@MapperScan(\"com.example.mapper\")\npublic class MybatisPlusConfig {\n    @Bean\n    public MybatisPlusInterceptor mybatisPlusInterceptor() {\n        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();\n        // 分页插件（必须加否则分页不生效）\n        interceptor.addInnerInterceptor(\n            new PaginationInnerInterceptor(DbType.MYSQL));\n        // 乐观锁\n        interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());\n        // 防全表更新删除（安全第一）\n        interceptor.addInnerInterceptor(new BlockAttackInnerInterceptor());\n        return interceptor;\n    }\n}\n\n// === QueryDSL 类型安全查询 ===\n// pom: querydsl-jpa, querydsl-apt（APT 生成 Q 类）\n@Service\npublic class UserQueryDSLService {\n    @Autowired private JPAQueryFactory queryFactory;  // Bean 配置过\n\n    public List<UserDTO> searchAdvanced(String nameKw, int minAge, String city) {\n        // QUser 是编译期 APT 自动生成的静态查询类，类型安全\n        QUser qUser = QUser.user;\n        QOrder qOrder = QOrder.order;\n\n        // JPA 风格复杂查询 + JOIN，字段写错编译报错\n        return queryFactory\n            .select(Projections.constructor(UserDTO.class,\n                qUser.id, qUser.name, qUser.age,\n                qOrder.amount.sum().as(\"totalSpent\")))\n            .from(qUser)\n            .leftJoin(qOrder).on(qOrder.customerId.eq(qUser.id))\n            .where(\n                qUser.name.like(\"%\" + nameKw + \"%\"),\n                qUser.age.goe(minAge),\n                qUser.city.eq(city)\n            )\n            .groupBy(qUser.id, qUser.name, qUser.age)\n            .orderBy(qOrder.amount.sum().desc())\n            .fetch();\n    }\n}\n\n// === Hibernate Reactive 响应式（WebFlux 场景）===\n// 依赖: hibernate-reactive-core, quarkus-hibernate-reactive-panache 或 spring-boot-starter-webflux + r2dbc\n/*\n@QuarkusApplication\npublic class ReactiveUserRepository implements PanacheRepository<User> {\n    // 响应式方法返回 Uni/Multi 而非直接对象\n    public Uni<User> findByEmailReactive(String email) {\n        return find(\"email\", email).firstResult();\n    }\n    public Multi<User> findByCityReactive(String city) {\n        return streamAll();  // 流式返回\n    }\n} */"
  },
  {
    "id": "java-ext5-security",
    "title": "Java 安全与现代框架实践",
    "level": "高级",
    "content": "**OAuth2 / OIDC + Spring Authorization Server 授权服务**\n- **OAuth2 授权框架（RFC 6749）**：解决第三方应用获取用户资源授权，无需用户密码。核心角色：Resource Owner（用户）、Client（第三方应用）、Authorization Server（授权服务器）、Resource Server（资源服务器）。\n- **四种授权模式**：1）Authorization Code（授权码，最安全，先 code 后 token）；2）Client Credentials（客户端凭证，后端服务间调用，无用户）；3）Password（直接用用户名密码换 token，已废弃，SPA/移动端用 PKCE 授权码）；4）Implicit（简化模式，token 放 URL hash，已废弃）。\n- **PKCE（Proof Key for Code Exchange，RFC 7636）**：授权码模式增强版，防 code 拦截攻击。客户端生成 code_verifier 随机串并取哈希 code_challenge 给授权服务器，拿 token 时带上 verifier 校验。SPA、移动端无 client_secret，必须用 PKCE。\n- **OIDC（OpenID Connect）= OAuth2 + 身份认证**：在 OAuth2 access_token 基础上加 id_token（JWT），携带用户身份信息（sub、name、email、iss、aud、exp 等）。用户端点 /userinfo 拿用户详情。SSO 单点登录基于 OIDC Session。\n- **Spring Authorization Server（正式版，替代旧 Spring Security OAuth）**：生产级授权服务器实现。配置：RegisteredClientRepository（客户端信息：clientId、clientSecret、redirectUri、授权模式、scope）、AuthorizationServerSettings（端点 URL、issuer）、SecurityFilterChain（表单登录、端点放行）。\n- **授权端点**：/oauth2/authorize（授权码请求）、/oauth2/token（换 token）、/oauth2/jwks（公钥 JWKS，资源服务器验签用）、/oauth2/revoke（撤销）、/.well-known/openid-configuration（OIDC 发现）。\n- **Spring OAuth2 Resource Server**：资源服务器（业务 API）。spring.security.oauth2.resourceserver.jwt.issuer-uri 配置授权服务器发行者，自动从 JWKS 拿公钥验签。JWT 解析 claims，@PreAuthorize(\"hasAuthority('SCOPE_orders.read')\") 按 scope 鉴权。\n- **Spring OAuth2 Client**：第三方登录（微信、GitHub 登录）。OAuth2LoginConfigurer 配置。基于 OIDC 的 SSO。\n\n**JWT 最佳实践**\n- **JWT 结构**：Header（alg、kid）+ Payload（claims）+ Signature。Base64URL 非加密，任何人可读，签名仅防篡改。\n- **签名算法**：HS256（对称，共享密钥）适合单体；RS256/ES256（非对称，私钥签名公钥验签）适合分布式授权服务器+资源服务器分离。选 RS256/ES256。\n- **Claims 标准字段**：iss（签发者）、sub（主体用户ID）、aud（受众）、exp（过期时间戳）、nbf（Not Before）、iat（签发时间）、jti（唯一ID，防重放）。自定义业务字段放 claims 但别放敏感信息（密码）。\n- **Access Token vs Refresh Token**：Access Token 短寿命（5-15 分钟），放请求头 Authorization: Bearer xxx；Refresh Token 长寿命（7-30 天），存在 HttpOnly Cookie 或安全存储，Access Token 过期后用 Refresh Token 换 Access Token。\n- **JWT 存储**：SPA：Access Token 放内存（变量），Refresh Token 放 HttpOnly + Secure + SameSite=Strict Cookie。原生 App：Keychain/安全存储。后端服务间：Client Credentials。不要放 localStorage（XSS 可偷）。\n- **撤销机制**：Short-lived token + 黑名单（Redis set jti，expire 匹配 token 过期时间）。或专门的 /oauth2/revoke 端点结合持久化 token 存储。\n- **常见坑**：算法为 none（空签名攻击）→ 验签前强制检查算法；密钥太弱 → 至少 32 字节 HS256；过期无限长 → 务必设 exp；接受任意签名 → 必须调用官方库验签方法。\n\n**CSRF / XSS 防护**\n- **XSS（跨站脚本攻击）**：注入恶意脚本到页面执行。反射型（URL 拼脚本）、存储型（留言板存脚本）、DOM 型（前端 innerHTML）。防护：1）输出 HTML 转义（Thymeleaf 默认转义，JS 模板用 safe 函数）；2）Content-Security-Policy 响应头限制脚本源：default-src 'self' script-src 'nonce-{random}'；3）HttpOnly Cookie（JS 读不到）；4）CSP nonce 禁止 inline 脚本。\n- **CSRF（跨站请求伪造）**：用户登录 A 网站未退出，恶意网站 B 自动发请求到 A 浏览器自动带 Cookie。防护：1）CSRF Token（Spring Security 默认开启，每次请求表单带 _csrf 参数或 X-XSRF-TOKEN 头，服务端校验）；2）SameSite=Strict/Lax Cookie 属性（Chrome 默认 SameSite=Lax，不随跨站 POST 发送）；3）自定义请求头（X-Requested-With: XMLHttpRequest，CORS 预检）；4）验证 Referer/Origin 头。\n- **Spring Security CSRF 配置**：前后端分离（JSON）推荐用 Cookie CSRF（CookieCsrfTokenRepository.withHttpOnlyFalse()），前端从 Cookie 读 XSRF-TOKEN 写请求头。注意：仅对状态改变请求（POST/PUT/DELETE/PATCH）校验，GET/HEAD/OPTIONS 忽略。纯 API 无浏览器访问可关 CSRF（如微服务间调用）。\n\n**Spring Security 6 方法级安全**\n- **@EnableMethodSecurity**（替代旧 @EnableGlobalMethodSecurity）。@PreAuthorize（方法执行前 SpEL 鉴权）、@PostAuthorize（执行后返回值检查）、@PreFilter（参数过滤）、@PostFilter（返回值过滤）。\n- **权限表达式**：hasAuthority('scope:admin') / hasRole('ADMIN')（带 ROLE_ 前缀）/ hasAnyRole(...) / isAuthenticated() / @permissionService.check(#id, authentication)。\n- **基于方法注解的细粒度权限**：@PreAuthorize(\"hasPermission(#orderId, 'ORDER', 'DELETE')\" + \" or @orderSecurity.isOwner(authentication, #orderId)\") 结合自定义 PermissionEvaluator。\n- **新特性**：@EnableMethodSecurity(jsr250Enabled = true, securedEnabled = true) 向后兼容 JSR-250 @RolesAllowed 和 @Secured。默认 prePostEnabled = true（@PreAuthorize 生效，旧版默认 false 要手动开）。",
    "example": "// === Spring Authorization Server 配置（授权服务器）===\n// 依赖: spring-boot-starter-oauth2-authorization-server\n@Configuration\n@EnableWebSecurity\npublic class AuthorizationServerConfig {\n\n    // 1. 授权服务器 SecurityFilterChain（优先于默认过滤链）\n    @Bean\n    @Order(Ordered.HIGHEST_PRECEDENCE)\n    public SecurityFilterChain authorizationServerSecurityFilterChain(HttpSecurity http) throws Exception {\n        OAuth2AuthorizationServerConfiguration.applyDefaultSecurity(http);\n        http.getConfigurer(OAuth2AuthorizationServerConfigurer.class)\n            .oidc(Customizer.withDefaults());  // 启用 OIDC（.well-known 等）\n        http.exceptionHandling(ex -> ex\n            .authenticationEntryPoint(new LoginUrlAuthenticationEntryPoint(\"/login\")));\n        // 允许 token 端点表单认证（client_secret_post）\n        http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()));\n        return http.formLogin(Customizer.withDefaults()).build();\n    }\n\n    // 2. 用户认证（表单登录用户名密码）\n    @Bean\n    public UserDetailsService userDetailsService() {\n        return new InMemoryUserDetailsManager(\n            User.withUsername(\"admin\")\n                .password(\"{bcrypt}$2a$10$...\")  // BCrypt 加密\n                .roles(\"ADMIN\")\n                .build(),\n            User.withUsername(\"user\")\n                .password(\"{noop}123456\")  // 明文（仅测试）\n                .roles(\"USER\")\n                .build());\n    }\n\n    // 3. 注册客户端信息\n    @Bean\n    public RegisteredClientRepository registeredClientRepository() {\n        // 授权码模式 + PKCE + OIDC 的公共客户端（SPA/移动端）\n        RegisteredClient pkceClient = RegisteredClient.withId(\"spa-client\")\n            .clientId(\"spa-frontend\")\n            .clientAuthenticationMethod(ClientAuthenticationMethod.NONE)  // 无 secret\n            .authorizationGrantType(AuthorizationGrantType.AUTHORIZATION_CODE)\n            .authorizationGrantType(AuthorizationGrantType.REFRESH_TOKEN)\n            .redirectUri(\"http://localhost:5173/callback\")\n            .scope(OidcScopes.OPENID)\n            .scope(OidcScopes.PROFILE)\n            .scope(\"orders.read\")\n            // PKCE 要求\n            .clientSettings(ClientSettings.builder()\n                .requireProofKey(true)  // 强制 PKCE\n                .requireAuthorizationConsent(false).build())\n            .tokenSettings(TokenSettings.builder()\n                .accessTokenTimeToLive(Duration.ofMinutes(15))\n                .refreshTokenTimeToLive(Duration.ofDays(7))\n                .idTokenSignatureAlgorithm(SignatureAlgorithm.RS256).build())\n            .build();\n\n        // Client Credentials 模式的后端服务客户端\n        RegisteredClient backendClient = RegisteredClient.withId(\"backend\")\n            .clientId(\"order-service\")\n            .clientSecret(\"{bcrypt}$2a$10$encodedSecretHash\")\n            .clientAuthenticationMethod(ClientAuthenticationMethod.CLIENT_SECRET_BASIC)\n            .authorizationGrantType(AuthorizationGrantType.CLIENT_CREDENTIALS)\n            .scope(\"inventory.read\")\n            .build();\n\n        return new InMemoryRegisteredClientRepository(pkceClient, backendClient);\n    }\n\n    // 4. 用非对称密钥对签 JWT（生产用 JWK Set keystore）\n    @Bean\n    public JWKSource<SecurityContext> jwkSource() {\n        RSAKey rsaKey = generateRsa();  // 从 keystore 加载或生成\n        JWKSet jwkSet = new JWKSet(rsaKey);\n        return new ImmutableJWKSet<>(jwkSet);\n    }\n}\n\n// === Spring OAuth2 Resource Server（业务 API）====\n@Configuration\n@EnableWebSecurity\n@EnableMethodSecurity  // 开启方法级 @PreAuthorize\npublic class ResourceServerConfig {\n    @Bean\n    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\n        http\n            .csrf(csrf -> csrf\n                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())\n                .csrfTokenRequestHandler(new CsrfTokenRequestAttributeHandler()))\n            .cors(Customizer.withDefaults())\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/public/**\").permitAll()\n                .requestMatchers(\"/admin/**\").hasRole(\"ADMIN\")\n                .anyRequest().authenticated())\n            // JWT 资源服务器：自动校验签名 + exp + iss\n            .oauth2ResourceServer(oauth2 -> oauth2\n                .jwt(jwt -> jwt.jwtAuthenticationConverter(\n                    jwtAuthenticationConverter())));\n        return http.build();\n    }\n\n    // JWT claims 转 Spring Security GrantedAuthority\n    private JwtAuthenticationConverter jwtAuthenticationConverter() {\n        JwtGrantedAuthoritiesConverter authoritiesConverter =\n            new JwtGrantedAuthoritiesConverter();\n        authoritiesConverter.setAuthorityPrefix(\"SCOPE_\");  // 默认前缀\n        authoritiesConverter.setAuthoritiesClaimName(\"scope\");\n        JwtAuthenticationConverter converter = new JwtAuthenticationConverter();\n        converter.setJwtGrantedAuthoritiesConverter(authoritiesConverter);\n        return converter;\n    }\n\n    // CORS 配置\n    @Bean\n    public CorsConfigurationSource corsConfigurationSource() {\n        CorsConfiguration config = new CorsConfiguration();\n        config.setAllowedOrigins(List.of(\"http://localhost:5173\"));\n        config.setAllowedMethods(List.of(\"GET\", \"POST\", \"PUT\", \"DELETE\"));\n        config.setAllowedHeaders(List.of(\"Authorization\", \"Content-Type\", \"X-XSRF-TOKEN\"));\n        config.setAllowCredentials(true);  // 跨域带 Cookie\n        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();\n        source.registerCorsConfiguration(\"/**\", config);\n        return source;\n    }\n}\n\n// === Controller：JWT claims 使用 + 方法级安全 ===\n@RestController\n@RequestMapping(\"/api/orders\")\npublic class OrderController {\n    @GetMapping\n    @PreAuthorize(\"hasAuthority('SCOPE_orders.read')\")  // 需要 scope\n    public List<OrderDTO> listOrders(@AuthenticationPrincipal Jwt jwt) {\n        String userId = jwt.getSubject();  // 用户 ID\n        List<String> scopes = jwt.getClaimAsStringList(\"scope\");\n        String email = jwt.getClaimAsString(\"email\");\n        return orderService.findByUserId(userId);\n    }\n\n    @DeleteMapping(\"/{orderId}\")\n    @PreAuthorize(\"hasRole('ADMIN') or @orderSecurity.isOwner(authentication, #orderId)\")\n    // 管理员 或 订单所有者 才能删除\n    public void deleteOrder(@PathVariable Long orderId) {\n        orderService.delete(orderId);\n    }\n}\n\n// 自定义权限判断组件\n@Component(\"orderSecurity\")\npublic class OrderSecurity {\n    @Autowired private OrderRepository orderRepo;\n    public boolean isOwner(Authentication auth, Long orderId) {\n        String currentUserId = auth.getName();\n        Order order = orderRepo.findById(orderId).orElse(null);\n        return order != null && order.getCustomerId().equals(currentUserId);\n    }\n}\n\n// === JJWT 生成与校验 JWT（不用 Spring 场景手动写）===\n// io.jsonwebtoken: jjwt-api / jjwt-impl / jjwt-jackson\n@Component\npublic class JwtService {\n    private final KeyPair keyPair;  // 生产从 keystore 加载\n    public JwtService() {\n        try {\n            KeyPairGenerator kpg = KeyPairGenerator.getInstance(\"RSA\");\n            kpg.initialize(2048);\n            this.keyPair = kpg.generateKeyPair();\n        } catch (NoSuchAlgorithmException e) { throw new RuntimeException(e); }\n    }\n\n    public String generateAccessToken(String userId, List<String> scopes) {\n        return Jwts.builder()\n            .header().keyId(\"key-1\").and()\n            .subject(userId)\n            .issuer(\"https://auth.example.com\")\n            .audience().add(\"order-service\").and()\n            .issuedAt(Date.from(Instant.now()))\n            .expiration(Date.from(Instant.now().plus(15, ChronoUnit.MINUTES)))\n            .id(UUID.randomUUID().toString())  // jti，黑名单用\n            .claim(\"scope\", String.join(\" \", scopes))\n            .claim(\"email\", userId + \"@example.com\")\n            .signWith(keyPair.getPrivate(), Jwts.SIG.RS256)  // 非对称签名\n            .compact();\n    }\n\n    public Claims validateToken(String token) {\n        try {\n            return Jwts.parser()\n                .verifyWith(keyPair.getPublic())  // 公钥验签\n                .requireIssuer(\"https://auth.example.com\")  // 校验 iss\n                .build()\n                .parseSignedClaims(token)\n                .getPayload();\n        } catch (JwtException e) {\n            // ExpiredJwtException / MalformedJwtException / SignatureException\n            throw new UnauthorizedException(\"无效 token: \" + e.getMessage());\n        }\n    }\n}\n\n// === XSS / CSP 安全头配置 ===\n@Bean\npublic SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n    http.headers(headers -> headers\n        .contentSecurityPolicy(csp -> csp\n            // CSP：只允许同源脚本 + 指定 nonce，禁止内联脚本/eval\n            .policyDirectives(\"default-src 'self'; \" +\n                \"script-src 'nonce-{nonce}' 'strict-dynamic'; \" +\n                \"style-src 'self' 'unsafe-inline'; \" +\n                \"img-src 'self' data: https://cdn.example.com; \" +\n                \"frame-ancestors 'none'; \" +\n                \"base-uri 'self'; \" +\n                \"form-action 'self';\"))\n        .httpStrictTransportSecurity(hsts -> hsts\n            // HTTPS 强制 1 年，含子域名，预加载\n            .maxAgeInSeconds(31536000).includeSubDomains(true).preload(true))\n        .frameOptions(frame -> frame.deny())  // 禁止 iframe 嵌套（ClickJacking）\n        .xssProtection(xss -> xss.enabled(true))  // 浏览器 XSS 过滤\n        .contentTypeOptions(opt -> {})  // 禁止 MIME sniffing\n    );\n    return http.build();\n}\n// Thymeleaf 模板输出默认自动 HTML 转义\n// <span th:text=\"${userInput}\"> --> 自动转义 <>&\"'\n// 脚本非转义输出（有风险）：th:utext=\"${...}\" 尽量避免\n\n// === Spring Security 6 方法级安全复杂场景 ===\n@Service\n@EnableMethodSecurity\npublic class DocumentService {\n\n    @PreAuthorize(\"@docPermission.canRead(authentication, #docId)\")\n    public Document getDocument(Long docId) {\n        return docRepo.findById(docId).orElseThrow();\n    }\n\n    // 返回值过滤：只返回当前用户有权看的内容\n    @PostAuthorize(\"@docPermission.canRead(authentication, returnObject.id)\")\n    public Document getByTitle(String title) {\n        return docRepo.findByTitle(title);\n    }\n\n    // 参数过滤：只接收用户自己的输入 ID\n    @PreFilter(\"@docPermission.canWrite(authentication, filterObject)\")\n    public void batchUpdate(List<Long> docIds) {\n        // docIds 进入方法前会被过滤，只剩下有权的\n        // ...\n    }\n\n    // 返回值过滤：集合\n    @PostFilter(\"@docPermission.canRead(authentication, filterObject.id)\")\n    public List<Document> search(String keyword) {\n        return docRepo.search(keyword);\n    }\n}"
  }
],
java_ext5_questions: [
  {
    "q": "Java Record 类的特性不包括？",
    "level": "高级",
    "options": ["自动生成全参构造器、getter、equals/hashCode/toString","字段默认 final，不可变","可以继承其他类","适合 DTO、值对象场景"],
    "answer": 2,
    "explain": "Record 类隐式为 final，不能继承其他类（只能实现接口），所有字段为 final，自动生成基于字段值的 equals/hashCode，是不可变数据载体。"
  },
  {
    "q": "Sealed Class 密封类通过哪个关键字限定允许继承的子类？",
    "level": "高级",
    "options": ["extenders","allows","permits","implements"],
    "answer": 2,
    "explain": "sealed class/interface 通过 permits 精确列出可继承/实现的子类。子类必须声明为 final（终结）、sealed（继续密封）或 non-sealed（打破密封，允许任意子类）。"
  },
  {
    "q": "Java 21 虚拟线程（Virtual Threads）的实现机制是？",
    "level": "高级",
    "options": ["操作系统内核线程的轻量别名","由 JVM 在少量 OS 载体线程上调度，阻塞自动卸载（mount/unmount）","协程切换不需要栈","用用户态线程库模拟"],
    "answer": 1,
    "explain": "虚拟线程是 JVM 管理的轻量级线程，在少量 OS 平台线程（载体线程）上调度，阻塞 IO 时自动卸载（让出载体线程），百万级虚拟线程轻松创建，API 与传统线程兼容。"
  },
  {
    "q": "Scoped Values 相比 ThreadLocal 的优势不包括？",
    "level": "高级",
    "options": ["不可变，作用域结束自动清除（无内存泄漏）","支持子线程/虚拟线程安全继承","可变值更灵活","百万虚拟线程场景内存占用低"],
    "answer": 2,
    "explain": "Scoped Values 设计为不可变，值绑定在作用域内运行，退出自动清除（无需 remove），解决 ThreadLocal 内存泄漏问题且扩展到虚拟线程。可变不是它的目标。"
  },
  {
    "q": "Java 21 Switch 模式匹配特性不包括？",
    "level": "高级",
    "options": ["支持类型模式（case Integer i）","支持守卫模式（when s.length() > 5）","支持 null 处理不 NPE","必须用 default 分支"],
    "answer": 3,
    "explain": "当 switch 结合 Sealed Class 密封类时，编译器可穷尽检查所有 permitted 子类，不需要 default 分支。类型模式、守卫 when、null 处理都是 Java 21 Switch 正式支持的特性。"
  },
  {
    "q": "Spring Boot 3.x 的 JDK 基线和命名空间变化是？",
    "level": "高级",
    "options": ["Java 8+ 不变，javax.* 保留","Java 11+，javax.* → jakarta.*","Java 17+，javax.* → jakarta.*","Java 21+，无命名空间变化"],
    "answer": 2,
    "explain": "Spring Boot 3.x 要求 Java 17+（LTS）且 Jakarta EE 9+ 命名空间：javax.servlet → jakarta.servlet、javax.persistence → jakarta.persistence 等迁移。"
  },
  {
    "q": "Spring Boot 3 AOT + GraalVM Native Image 的主要优势是？",
    "level": "高级",
    "options": ["构建更快，运行峰值性能更高","启动毫秒级、内存占用低（适合 Serverless/快速扩容）","不需要任何配置直接运行","支持所有动态代理无需声明"],
    "answer": 1,
    "explain": "Native Image 运行时启动极快（<100ms）内存占用低。但构建慢、不支持完全动态（反射/代理需 Runtime Hints 声明），峰值性能通常比 JIT JVM 略低 10-30%。"
  },
  {
    "q": "Spring Boot 3 ProblemDetails 实现了哪个标准？",
    "level": "高级",
    "options": ["RFC 7231 HTTP 语义","RFC 7807 Problem Details for HTTP APIs","JSR 380 Bean Validation","OAuth2 RFC 6749"],
    "answer": 1,
    "explain": "ProblemDetails 按 RFC 7807 标准化 API 错误响应，统一返回 type/title/status/detail/instance 字段，以及自定义扩展字段，替代项目各自实现的统一响应类。"
  },
  {
    "q": "Spring 6 HTTP Interface 声明式 HTTP 客户端的特点是？",
    "level": "高级",
    "options": ["需要 Feign 第三方依赖","接口 + @GetExchange/@PostExchange 注解，Spring 官方内置实现","只支持响应式调用","不能配置拦截器"],
    "answer": 1,
    "explain": "HTTP Interface 是 Spring 6 官方内置（无需 Feign）。定义接口 + @HttpExchange/@GetExchange/@PostExchange 注解，HttpServiceProxyFactory 生成代理，支持 RestClient/WebClient 底层。"
  },
  {
    "q": "Spring 6 RestClient 相比 RestTemplate 的优势是？",
    "level": "高级",
    "options": ["异步非阻塞响应式","Fluent API 流式调用、更现代易用、更细粒度错误处理","底层基于 OkHttp","支持 HTTP/2 多路复用"],
    "answer": 1,
    "explain": "RestClient 是 Spring 6 新出的阻塞式 HTTP 客户端，替代 RestTemplate。Fluent 链式 API（uri/retrieve/onStatus/body），用法类似 WebClient 但同步，更现代易用。"
  },
  {
    "q": "Micrometer 观测三大支柱不包括？",
    "level": "高级",
    "options": ["Metrics（指标聚合数值）","Tracing（分布式链路）","Logging（带上下文日志）","Deploying（部署统计）"],
    "answer": 3,
    "explain": "可观测性三大支柱为 Metrics（指标，可聚合告警）、Logging（日志，事件详情）、Tracing（链路，跨服务调用关系）。Micrometer 统一这三者 API，Spring Boot 3 一等公民。"
  },
  {
    "q": "Micrometer Timer 与 Counter 的区别是？",
    "level": "高级",
    "options": ["无区别","Counter 是单调递增计数，Timer 同时记录次数 + 耗时 + 分位数直方图","Counter 记录耗时，Timer 计数","Timer 不能导出 Prometheus"],
    "answer": 1,
    "explain": "Counter 是单调递增计数器（请求总数）；Timer 自动维护 count（次数）+ sum（总耗时）+ bucket（直方图用于 P99 分位数），适合接口耗时统计。"
  },
  {
    "q": "@Observed 注解的作用是？",
    "level": "高级",
    "options": ["日志打印","自动 Micrometer 观测：Timer 指标 + Observation Tracing（Span），AOP 拦截","事务管理","请求限流"],
    "answer": 1,
    "explain": "@Observed（需 micrometer-observation-aop）通过 AOP 自动包装方法：创建 Observation，记录 Timer 指标，如引入 tracing 依赖还会自动生成 Span，零侵入实现可观测。"
  },
  {
    "q": "K8s Readiness Probe（就绪探针）失败时的行为是？",
    "level": "高级",
    "options": ["重启容器","Service 摘除流量，不给该 Pod 发请求","删除 Pod 重建","不影响"],
    "answer": 1,
    "explain": "Readiness 判断容器能否接收流量，失败时 K8s 将 Pod IP 从 Service Endpoints 中移除（摘流），等恢复再加回。Liveness 失败才重启容器。"
  },
  {
    "q": "Docker Spring Boot 分层镜像构建的核心目的是？",
    "level": "高级",
    "options": ["减小镜像体积","利用 Docker layer cache，依赖不频繁变的层先复制，加快重复构建","提高启动速度","简化 Dockerfile"],
    "answer": 1,
    "explain": "分层 JAR 将内容分成 dependencies（最不常变）→ spring-boot-loader → snapshot-deps → application（最常变）。每层独立镜像层，代码改时只重新构建 application 层，其他层用缓存，构建更快。"
  },
  {
    "q": "JVM 在容器中识别内存限制后，默认堆内存是容器限制的？",
    "level": "高级",
    "options": ["50%","25%（MaxRAMPercentage=25）","90%","无限制"],
    "answer": 1,
    "explain": "Java 10+ UseContainerSupport 自动读取 CGroup 限制，默认 MaxRAMPercentage=25（老版 1/4）。容器 1GB 内存则堆 ~250MB，生产可通过 -XX:MaxRAMPercentage=75 调大但留空间给堆外。"
  },
  {
    "q": "Helm Chart 中 values.yaml 的作用是？",
    "level": "高级",
    "options": ["存储密码密钥","模板默认配置值，可通过 -f/--set 覆盖，实现一套模板多环境部署","K8s API Server 配置","Docker 标签管理"],
    "answer": 1,
    "explain": "values.yaml 是 Helm Chart 默认参数，模板中 {{ .Values.replicaCount }} 引用。helm install -f prod-values.yaml 或 --set 命令行覆盖，实现一套模板复用在 dev/stg/prod。"
  },
  {
    "q": "Spring Cloud Kubernetes Config 自动热刷新 @RefreshScope Bean 的条件是？",
    "level": "高级",
    "options": ["删除 Pod 重建","spring.cloud.kubernetes.reload.enabled=true 并监听 ConfigMap 变化","调用 Spring Boot restart 端点","修改 application.properties 后刷新浏览器"],
    "answer": 1,
    "explain": "Spring Cloud Kubernetes Reload 功能监听 ConfigMap/Secret 变化，触发 Context refresh 或 Environment update，@RefreshScope Bean 重新创建获取新配置，无需重启。"
  },
  {
    "q": "Prometheus 指标采集模型是？",
    "level": "高级",
    "options": ["应用 Push 到 Prometheus","Prometheus 定时 Pull（scrape）应用端点指标","消息队列中转","数据库轮询"],
    "answer": 1,
    "explain": "Prometheus 默认拉模型（Pull）：服务发现后周期性 GET /actuator/prometheus 等暴露端点。适合防火墙内监控，避免应用推送导致的端口暴露问题。"
  },
  {
    "q": "OpenTelemetry Java Agent 自动埋点的特点是？",
    "level": "高级",
    "options": ["修改业务代码","无侵入 java -javaagent:opentelemetry-javaagent.jar，自动拦截 Spring/MVC/Kafka/HTTP 等","需要写大量拦截器代码","只支持 metrics 不支持 tracing"],
    "answer": 1,
    "explain": "OpenTelemetry Java Agent 以 -javaagent 形式附加到 JVM 启动命令，字节码增强自动对 Spring Web/Controllers、JPA、HTTP Client、Kafka、Logging（注入 traceId）等组件埋点，业务代码零改动。"
  },
  {
    "q": "Arthas trace 命令的作用是？",
    "level": "高级",
    "options": ["打印 JVM 线程栈","追踪方法调用链及每个子方法耗时，找到性能瓶颈","统计 SQL 执行计划","查看堆内存直方图"],
    "answer": 1,
    "explain": "trace com.foo.Service method 输出方法调用链上每个子方法调用的耗时（含次数、平均、最大），快速定位哪个环节慢。支持按耗时过滤 '#cost > 500'。"
  },
  {
    "q": "Arthas profiler start -e itimer 模式生成火焰图用于？",
    "level": "高级",
    "options": ["分析锁竞争","采样 CPU 执行热点函数定位 CPU 占用高的代码","分析 IO 阻塞","统计对象分配"],
    "answer": 1,
    "explain": "profiler -e itimer（CPU 模式）采样线程在用户态 CPU 上执行的函数栈，生成火焰图看热点方法占比，定位 CPU 消耗高的代码。wall 模式含阻塞时间适合找 IO 瓶颈。"
  },
  {
    "q": "GCEasy GC 日志分析报告指标，哪项说明可能内存泄漏？",
    "level": "高级",
    "options": ["YGC 每秒一次","Full GC 后老年代占用逐次升高且无法降到 80% 以下","MetaSpace 使用率 70%","YGC 耗时 30ms"],
    "answer": 1,
    "explain": "健康 JVM Full GC 后老年代会大幅回落。如果每次 Full GC 后老年代基线越来越高，说明存在无法回收的对象（内存泄漏），需要 MAT 分析堆。"
  },
  {
    "q": "Spring Data JPA Specification 的主要用途是？",
    "level": "高级",
    "options": ["分页排序","动态多条件组合查询（前端任意筛选条件）","批量插入","DDL 建表"],
    "answer": 1,
    "explain": "Specification 封装 JPA Criteria API，多个 Predicate 用 and/or 动态组合，适合多条件查询（名称模糊+日期范围+状态下拉等任意组合），比写多个方法名查询灵活。"
  },
  {
    "q": "MyBatis-Plus LambdaQueryWrapper 相比普通 QueryWrapper 的优势是？",
    "level": "高级",
    "options": ["执行更快","类型安全，字段用方法引用，列名拼写错误编译期发现","支持联表","不需要写 SQL"],
    "answer": 1,
    "explain": "LambdaQueryWrapper<User>().eq(User::getName, name) 用方法引用指定字段，而不是字符串 \"name\"，字段重命名或拼写错误在编译期报错，避免运行时 SQL 错误。"
  },
  {
    "q": "MyBatis-Plus BlockAttackInnerInterceptor 的作用是？",
    "level": "高级",
    "options": ["防止 SQL 注入","拦截无 WHERE 条件的全表 UPDATE/DELETE，防止误操作","分页拦截","多租户过滤"],
    "answer": 1,
    "explain": "BlockAttackInnerInterceptor 是 SQL 安全防护插件，执行 UPDATE/DELETE 语句时如果解析不到 WHERE 条件（update t_user set name = 'x' 会更新全表）直接抛异常，防止手滑灾难。"
  },
  {
    "q": "OAuth2 授权码模式 + PKCE 相比纯授权码模式主要解决什么问题？",
    "level": "高级",
    "options": ["简化流程","防止授权码被中间人拦截重放攻击（PKCE 用 verifier/challenge 校验）","支持资源所有者密码","不需要 client_secret"],
    "answer": 1,
    "explain": "PKCE（RFC 7636）客户端生成 code_verifier 并哈希为 code_challenge 发给授权服务器，换 token 时带 verifier 校验。即使 code 被网络拦截没有 verifier 也换不到 token，保护公共客户端（SPA/移动端）。"
  },
  {
    "q": "JWT 最佳实践中，Access Token 与 Refresh Token 存储方式推荐？",
    "level": "高级",
    "options": ["都放 localStorage","Access Token 放前端内存变量，Refresh Token 放 HttpOnly+Secure+SameSite Cookie","都放 Cookie（可被 CSRF 利用）","都放 SessionStorage"],
    "answer": 1,
    "explain": "Access Token 短寿命放内存（防 XSS 窃取持久化），Refresh Token 长寿命放 HttpOnly Cookie（JS 读不到，防 XSS），且 Secure + SameSite=Strict 降低 CSRF 风险。"
  },
  {
    "q": "Spring Security 6 中替代旧 @EnableGlobalMethodSecurity 的注解是？",
    "level": "高级",
    "options": ["@EnableSecurity","@EnableMethodSecurity","@EnableGlobalSecurity","@MethodAuth"],
    "answer": 1,
    "explain": "Spring Security 6 推荐 @EnableMethodSecurity，默认开启 prePostEnabled（@PreAuthorize/@PostAuthorize），可配 jsr250Enabled / securedEnabled 兼容旧注解。更灵活，自定义 PermissionEvaluator。"
  },
  {
    "q": "Spring Security 默认开启的 CSRF 防护，前后端分离 API 场景推荐做法？",
    "level": "高级",
    "options": ["直接关闭 CSRF（所有 API 都无浏览器访问才安全）","CookieCsrfTokenRepository.withHttpOnlyFalse()：Cookie 下发 XSRF-TOKEN，前端读 Cookie 写 X-XSRF-TOKEN 请求头","Session 存 token 前端取不到","用 JWT 不需要 CSRF"],
    "answer": 1,
    "explain": "前后端分离且有浏览器访问（Cookie 会话）推荐 CookieCsrfTokenRepository：HttpOnly=false 允许前端 JS 从 Cookie 读取 token，放入 X-XSRF-TOKEN 头，服务端默认从该头校验。纯微服务间调用无浏览器才能关 CSRF。"
  }
]
